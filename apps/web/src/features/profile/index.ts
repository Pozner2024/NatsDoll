// Public API — export only what consumers of this feature should access

// Components
// export { ProfileHeader, ProfileOrders } from './components'

// Composables
// export { useProfile } from './composables'

// No store for profile — read-only data
